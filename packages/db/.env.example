# Local dev (docker-compose): postgresql://vampchains:vampchains@localhost:5432/vampchains
# Neon (prod): use the pooled connection string from the Neon dashboard.
DATABASE_URL="postgresql://vampchains:vampchains@localhost:5432/vampchains"
