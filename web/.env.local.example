# Contract addresses from `forge script script/Deploy.s.sol` output
NEXT_PUBLIC_REGISTRY_ADDRESS=""
NEXT_PUBLIC_BRIDGE_ADDRESS=""
NEXT_PUBLIC_USDC_ADDRESS=""
NEXT_PUBLIC_USDC_DECIMALS="6"

# Home chain (local anvil for dev; Base mainnet/Sepolia in prod)
NEXT_PUBLIC_L1_CHAIN_ID="31337"
NEXT_PUBLIC_L1_RPC_URL="http://localhost:8545"

# infra/rpc-gateway's public URL
NEXT_PUBLIC_GATEWAY_URL="http://localhost:18080"

# Postgres (shared with @vampchains/db)
DATABASE_URL="postgresql://vampchains:vampchains@localhost:5432/vampchains"
